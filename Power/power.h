#pragma once
#include "defines.h"

namespace math {

	double power(double _base, int _exponent);
	unsigned long long power2(unsigned int _base, int _exponent);
	// ***************** <<_USAGE_>> *****************
	void powerUsage();

}; // math