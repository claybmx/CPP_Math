//#include "vld.h" // (ignore LIBCD.lib) No memory leaks DETECTED!
#include "fatoresPrimos.h"

int main() {

	utils::Console cmd(95, 50, "<<_FATORES_PRIMOS_>>");

	math::fatoresPrimosUsage();

	system("pause");
	return(0);
}